# AGENTS

Automated agents and CLI runners must treat this repository as a non-coder environment. Always explain rationale and implementation details explicitly when proposing or applying changes. Review `INSTRUCTIONS/AGENT_DOCS_CHECKLIST.md` before editing, keep docs in sync with code, and log updates in `docs/CHANGE_LOG.md`. If uncertain about existing context, pause and ask for clarification instead of making assumptions.
